name: Working with Inputs

on:  
  workflow_dispatch: 
    inputs:
      num1: 
       type: number
       description: un numero
      num2: 
       type: number
       description: otro numero
      
jobs:
  build:
   # outputs:
   #   variableSalida: "mi Valor"
    runs-on: ubuntu-latest
    steps:
      - name: Suma
        run: echo "SUMA=$(( ${{ inputs.num1 }} + ${{ inputs.num2 }} ))"

 # deploy:
  #  runs-on: ubuntu-latest
  #  needs: build      
  #  steps:
   #   - name: Deploy
    #    run: echo "Variable es ${{needs.build.outputs.variableSalida}}" 
